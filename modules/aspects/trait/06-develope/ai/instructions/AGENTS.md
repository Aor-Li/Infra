# 全局 agent 指令

挂到 `~/.claude/CLAUDE.md` 与 `~/.codex/AGENTS.md`（out-of-store symlink，直接改
即刻生效）。只放**跨项目、跨 agent 都成立**的约定——项目专属的写进各仓库自己的
`AGENTS.md`，某个 agent 独有的写进它自己的配置。

## 语言

- 对话用中文；代码、标识符、commit message 用英文。

## 待补

这份文件刻意留白。往里加东西之前先问：这条约定在**另一个仓库、另一个 agent** 上
还成立吗？不成立就说明它该待在别处。
